# Dedicated Memory MCP — declined

A sixth MCP server for timestamps, promote/demote, and forget-horizon GC is the wrong shape.

Continuity across isolated heartbeats is OpenClaw **scratch**, not retrieval. Dated-note expiry is a `healthcheck.py` sweep. Durable facts stay in `MEMORY.md`.

Canonical plan: [`AGENT_MEMORY.md`](AGENT_MEMORY.md).
