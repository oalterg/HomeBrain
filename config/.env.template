COMPOSE_PROJECT_NAME=homebrain
MASTER_PASSWORD=
# Deployment mode: "local" for LAN-only access (no tunnel, no registration required)
# Set to "remote" to enable Pangolin or Cloudflare tunnel for external access
DEPLOYMENT_MODE=local
MANAGER_PASSWORD=
MANAGER_DOMAIN=
# --- Tunnel Configuration ---
# Only required if DEPLOYMENT_MODE=remote
PANGOLIN_DOMAIN=
# Only required if DEPLOYMENT_MODE=remote
PANGOLIN_ENDPOINT=
# Only required if DEPLOYMENT_MODE=remote
NEWT_ID=
# Only required if DEPLOYMENT_MODE=remote
NEWT_SECRET=
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=
NEXTCLOUD_TRUSTED_DOMAINS=
NEXTCLOUD_PORT=8080
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud_user
MYSQL_PASSWORD=
NEXTCLOUD_DATA_DIR=
BACKUP_RETENTION=8
BACKUP_MINUTE=0
BACKUP_HOUR=3
BACKUP_DAY_MONTH=*
BACKUP_MONTH=*
BACKUP_DAY_WEEK=0
# Only required if DEPLOYMENT_MODE=remote (Cloudflare)
CF_TUNNEL_ID=
TRUSTED_PROXIES_0=172.18.0.1
TRUSTED_PROXIES_1=127.0.0.1
HA_PORT=8123
HA_TRUSTED_DOMAINS=
HA_ADMIN_PASSWORD=
# Only required if DEPLOYMENT_MODE=remote (Cloudflare)
CF_TOKEN_NC=
# Only required if DEPLOYMENT_MODE=remote (Cloudflare)
CF_TOKEN_HA=